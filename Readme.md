React
#parcel 
-dev build
-local server
-hmr=hot module replacement
-file watching algorith -written in c++
-image optimization
-bundlimg
-compressing
-differential bundling-support older bbrower
-tree shaking -remove unused code




mock page

/**
 * header
 * -logo
 * -nav items
 * body
 * -search
 * -restaurant container
 *     -img
 *     -name of res,star rating,
 * footer
 * -copyrights
 * links
 * address
 * contact
 */





 #react Toolkit
   install @redux/toolkit and react-redux
   build our store 
   connect our store to our app
   slice(cardslice)
   dispatch
   selector




  #types of testing for developer
   - unit testing(test component by component)
   -intergration testing(`test component that intract with other component)
   -end to end testing -e2e testing(test entire flow of the app as a user ) we need tool to testing


  react testing liberary -> uses jest behind the scene 




  #setting up testing in our app
  - install react testing lib,jest,babel-jest,
  -config babel 
  -config parcel config file to disable default babel transplilation
  -jest config   npx jest --init
  -instal  jsdom lib
  

  
